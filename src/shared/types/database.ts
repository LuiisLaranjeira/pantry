// Placeholder. Replace with output of:
//   npx supabase gen types typescript --project-id <id> > src/shared/types/database.ts
// Tracked in Phase 6 (Supabase hardening).

export type Database = unknown;
